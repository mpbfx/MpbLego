# 场景代码

### <font style="color:#C75C00;">CSS 实现类</font>

#### 用 CSS 将正三角形变成倒三角形

**核心原理：** 利用 `border` 实现 CSS 三角形，改变边的颜色即可改变方向。

```css
/* 正三角形（向上） */
.triangle-up {
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-bottom: 10px solid black;
}

/* 倒三角形（向下） */
.triangle-down {
  width: 0;
  height: 0;
  border-left: 10px solid transparent;
  border-right: 10px solid transparent;
  border-top: 10px solid black;
}
```

#### 用纯CSS绘制一个五角星

```css
<!DOCTYPE html>
<div style="
  width:0;
  height:0;
  border-right:50px solid transparent;
  border-bottom:100px solid gold;
  border-left:50px solid transparent;
  transform:rotate(35deg);
  position:relative;
">
  <div style="
    content:'';
    width:0;
    height:0;
    border-right:15px solid transparent;
    border-bottom:100px solid gold;
    border-left:15px solid transparent;
    position:absolute;
    top:-60px;
    left:-30px;
    transform:rotate(-35deg);
  "></div>
  <div style="
    content:'';
    width:0;
    height:0;
    border-right:50px solid transparent;
    border-bottom:100px solid gold;
    border-left:50px solid transparent;
    position:absolute;
    top:-35px;
    left:-50px;
    transform:rotate(-70deg);
  "></div>
</div>
```

#### 用CSS实现一个扇形进度条

```css
<!DOCTYPE html>
<div style="
  width:100px;
  height:100px;
  border-radius:50%;
  background:conic-gradient(orange 0% 75%, #eee 75% 100%);
  position:relative;
">
  <div style="
    width:60px;
    height:60px;
    background:white;
    border-radius:50%;
    position:absolute;
    top:20px;
    left:20px;
  "></div>
  <div style="
    position:absolute;
    top:50%;
    left:50%;
    transform:translate(-50%,-50%);
  ">75%</div>
</div>
```

#### 用CSS实现自适应九宫格布局

```css
<!DOCTYPE html>
<div style="
  display:grid;
  grid-template-columns:repeat(3, 1fr);
  gap:10px;
  width:300px;
  height:300px;
">
  <div style="background:#3498db"></div>
  <div style="background:#2ecc71"></div>
  <div style="background:#e74c3c"></div>
  <div style="background:#f1c40f"></div>
  <div style="background:#9b59b6"></div>
  <div style="background:#1abc9c"></div>
  <div style="background:#e67e22"></div>
  <div style="background:#34495e"></div>
  <div style="background:#7f8c8d"></div>
</div>
```

#### 用CSS实现一个不断旋转的加载动画

```css
<!DOCTYPE html>
<div style="
  width:40px;
  height:40px;
  border:4px solid #f3f3f3;
  border-top:4px solid #3498db;
  border-radius:50%;
  animation:spin 1s linear infinite;
"></div>

<style>
@keyframes spin {
  0% { transform:rotate(0deg); }
  100% { transform:rotate(360deg); }
}
</style>
```

### <font style="color:#F3BB2F;">Vue/React 组件类</font>

#### 用 Vue 或 React 写一个倒计时组件

**解题思路：** 倒计时本质上是一个定时器（`setInterval` 或 `requestAnimationFrame`），在组件挂载时启动，定时更新当前剩余时间，倒计时结束后清理定时器。

**Vue 示例（Composition ****API****）**

```vue
<template>
  <div>{{ time }}</div>
</template>

<script setup lang="ts">
  import { ref, onMounted, onUnmounted } from 'vue'

  const duration = 60 // 秒数
  const time = ref(duration)
  let timer: number | null = null

  const startCountdown = () => {
    timer = window.setInterval(() => {
      if (time.value > 0) {
        time.value--
      } else {
        clearInterval(timer!)
      }
    }, 1000)
  }

  onMounted(() => startCountdown())
  onUnmounted(() => timer && clearInterval(timer))
</script>
```

#### 用 Vue 写一个 TodoList

**实现点：**

* 数据响应式管理
* 双向绑定
* 列表增删功能

```vue
<template>
  <div>
    <input v-model="newItem" @keyup.enter="addItem" />
    <ul>
      <li v-for="(item, index) in todos" :key="index">
        {{ item }} <button @click="removeItem(index)">删除</button>
      </li>
    </ul>
  </div>
</template>

<script setup>
  import { ref } from 'vue'

  const newItem = ref('')
  const todos = ref<string[]>([])

    function addItem() {
      if (newItem.value) {
        todos.value.push(newItem.value)
        newItem.value = ''
      }
    }

  function removeItem(index: number) {
    todos.value.splice(index, 1)
  }
</script>
```

#### 手写 Tooltip 组件

**思路要点：**

* 悬停时显示提示内容
* 支持自定义位置
* 使用 `position: absolute` + `ref` 定位提示内容

**React 简写示例：**

```jsx
const Tooltip = ({ content, children }) => {
  const [visible, setVisible] = useState(false)

  return (
    <div style={{ position: 'relative', display: 'inline-block' }}
      onMouseEnter={() => setVisible(true)}
      onMouseLeave={() => setVisible(false)}>
      {children}
      {visible && (
        <div style={{
          position: 'absolute',
          top: '100%',
          left: '0',
          background: 'black',
          color: 'white',
          padding: '4px 8px',
          borderRadius: '4px'
        }}>
          {content}
        </div>
      )}
    </div>
  )
}
```

#### 用 Vue 3 Composition API 或 React Hooks 实现一个图片懒加载组件

```css
<template>
  <img :src="isVisible ? src : placeholder" @load="onLoad" ref="imgRef" />
</template>

<script setup>
import { ref, onMounted } from 'vue'

const props = defineProps(['src'])
const imgRef = ref()
const isVisible = ref(false)
const placeholder = 'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7'

onMounted(() => {
  const observer = new IntersectionObserver(([entry]) => {
    if (entry.isIntersecting) {
      isVisible.value = true
      observer.disconnect()
    }
  })
  observer.observe(imgRef.value)
})

const onLoad = () => {
  // 图片加载完成后的处理
}
</script>
```

#### 用 Vue 或 React 实现一个可拖拽调整宽高的面板（resizable panel）

```css
<template>
  <div :style="{width: width + 'px', height: height + 'px'}" class="resizable-panel">
    <div class="resizer" @mousedown="startResize"></div>
  </div>
</template>

<script setup>
import { ref, onMounted, onUnmounted } from 'vue'

const width = ref(200)
const height = ref(200)
let isResizing = false

const startResize = (e) => {
  isResizing = true
  const startX = e.pageX
  const startY = e.pageY
  const startWidth = width.value
  const startHeight = height.value
  
  const doResize = (e) => {
    if (!isResizing) return
    width.value = startWidth + (e.pageX - startX)
    height.value = startHeight + (e.pageY - startY)
  }
  
  const stopResize = () => {
    isResizing = false
    document.removeEventListener('mousemove', doResize)
    document.removeEventListener('mouseup', stopResize)
  }
  
  document.addEventListener('mousemove', doResize)
  document.addEventListener('mouseup', stopResize)
}
</script>

<style>
.resizable-panel {
  border: 1px solid #ccc;
  position: relative;
}
.resizer {
  position: absolute;
  right: 0;
  bottom: 0;
  width: 10px;
  height: 10px;
  background: blue;
  cursor: se-resize;
}
</style>
```

#### 用 Vue 或 React 实现一个搜索下拉联想组件（支持防抖）

```css
<template>
  <div>
    <input v-model="keyword" @input="handleInput" />
    <ul v-if="suggestions.length">
      <li v-for="item in suggestions" :key="item" @click="selectItem(item)">
        {{ item }}
      </li>
    </ul>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { debounce } from 'lodash-es'

const keyword = ref('')
const suggestions = ref([])
let timer = null

const handleInput = debounce(async () => {
  if (!keyword.value) {
    suggestions.value = []
    return
  }
  // 模拟API调用
  const res = await fetch(`/api/search?q=${keyword.value}`)
  suggestions.value = await res.json()
}, 300)

const selectItem = (item) => {
  keyword.value = item
  suggestions.value = []
}
</script>
```

#### 用 Vue 或 React 实现一个可折叠的树形菜单（Tree）

```css
import React, { useState } from 'react'

const TreeNode = ({ node }) => {
  const [isOpen, setIsOpen] = useState(false)
  
  return (
    <div style={{ marginLeft: '20px' }}>
      <div onClick={() => setIsOpen(!isOpen)}>
        {node.children ? (isOpen ? '▼' : '▶') : ''} {node.name}
      </div>
      {isOpen && node.children?.map(child => (
        <TreeNode key={child.id} node={child} />
      ))}
    </div>
  )
}

// 使用示例
const treeData = {
  name: 'Root',
  children: [
    { name: 'Child 1', children: [
      { name: 'Grandchild 1' },
      { name: 'Grandchild 2' }
    ]},
    { name: 'Child 2' }
  ]
}

const TreeMenu = () => <TreeNode node={treeData} />
```

### <font style="color:#01B2BC;">原生 JavaScript 实现</font>

#### 写一个 Input 组件（可复用）

**关键点：**

* 绑定 value 与事件
* 支持 props 下发（placeholder、type、onInput）

```vue
<template>
  <input :value="modelValue" @input="$emit('update:modelValue', $event.target.value)"
    :placeholder="placeholder" :type="type" />
</template>

<script setup>
  defineProps({
    modelValue: String,
    placeholder: String,
    type: { type: String, default: 'text' }
  })

  defineEmits(['update:modelValue'])
</script>
```

#### 写一个表单校验（原生 + Element Plus UI）

**原生校验：**

```vue
<form onsubmit="return validate()">
  <input id="email" />
  <span id="error"></span>
  <button type="submit">提交</button>
</form>

<script>
  function validate() {
    const value = document.getElementById('email').value
    if (!value.includes('@')) {
      document.getElementById('error').innerText = '请输入合法邮箱'
      return false
    }
    return true
  }
</script>
```

**Element Plus + Vue 校验：**

```vue
<el-form :model="form" :rules="rules" ref="formRef">
  <el-form-item label="邮箱" prop="email">
    <el-input v-model="form.email"></el-input>
  </el-form-item>
</el-form>

<script setup>
  import { reactive, ref } from 'vue'

  const form = reactive({ email: '' })
  const formRef = ref()
  const rules = {
    email: [{ required: true, message: '请输入邮箱', trigger: 'blur' },
            { type: 'email', message: '邮箱格式不正确', trigger: 'blur' }]
  }
</script>
```

### <font style="color:#0C68CA;">UI 组件手写实现</font>

#### 手写一个简单的 Modal 对话框组件（可遮罩、可关闭）

```css
<!DOCTYPE html>
<div id="modal" style="
  position:fixed;
  top:0; left:0; right:0; bottom:0;
  background:rgba(0,0,0,0.5);
  display:none;
  justify-content:center;
  align-items:center;
">
  <div style="
    background:white;
    padding:20px;
    border-radius:8px;
    position:relative;
  ">
    <button onclick="closeModal()" style="
      position:absolute;
      top:10px;
      right:10px;
      border:none;
      background:none;
      font-size:20px;
      cursor:pointer;
    ">×</button>
    <h3>标题</h3>
    <p>内容...</p>
  </div>
</div>

<button onclick="openModal()">打开Modal</button>

<script>
function openModal() { document.getElementById('modal').style.display = 'flex' }
function closeModal() { document.getElementById('modal').style.display = 'none' }
</script>
```

#### 手写一个下拉刷新组件（移动端）

```css
<!DOCTYPE html>
<div id="refreshContainer" style="
  height:100vh;
  overflow-y:auto;
  -webkit-overflow-scrolling:touch;
">
  <div id="refreshIndicator" style="
    text-align:center;
    padding:10px;
    display:none;
  ">下拉刷新</div>
  <div id="content">内容...</div>
</div>

<script>
let startY = 0
const container = document.getElementById('refreshContainer')
const indicator = document.getElementById('refreshIndicator')

container.addEventListener('touchstart', e => {
  if (container.scrollTop === 0) {
    startY = e.touches[0].pageY
  }
})

container.addEventListener('touchmove', e => {
  if (container.scrollTop === 0) {
    const diff = e.touches[0].pageY - startY
    if (diff > 0) {
      indicator.style.display = 'block'
      indicator.textContent = diff > 50 ? '松开刷新' : '下拉刷新'
    }
  }
})

container.addEventListener('touchend', async () => {
  if (indicator.textContent === '松开刷新') {
    indicator.textContent = '刷新中...'
    await new Promise(resolve => setTimeout(resolve, 1000)) // 模拟请求
    document.getElementById('content').innerHTML = '刷新时间: ' + new Date().toLocaleTimeString()
  }
  indicator.style.display = 'none'
})
</script>
```

#### 手写一个图片预览组件（点击放大、切换）

```css
<!DOCTYPE html>
<img src="image1.jpg" onclick="openPreview(this)" style="width:100px;">
<img src="image2.jpg" onclick="openPreview(this)" style="width:100px;">

<div id="preview" style="
  position:fixed;
  top:0; left:0; right:0; bottom:0;
  background:black;
  display:none;
  justify-content:center;
  align-items:center;
">
  <img id="previewImg" style="max-width:90%; max-height:90%;">
  <button onclick="changeImage(-1)" style="
    position:absolute;
    left:20px;
    top:50%;
    transform:translateY(-50%);
  ">❮</button>
  <button onclick="changeImage(1)" style="
    position:absolute;
    right:20px;
    top:50%;
    transform:translateY(-50%);
  ">❯</button>
  <button onclick="closePreview()" style="
    position:absolute;
    top:20px;
    right:20px;
  ">×</button>
</div>

<script>
let images = ['image1.jpg', 'image2.jpg', 'image3.jpg']
let currentIndex = 0

function openPreview(img) {
  currentIndex = images.indexOf(img.src.split('/').pop())
  document.getElementById('previewImg').src = images[currentIndex]
  document.getElementById('preview').style.display = 'flex'
}

function closePreview() {
  document.getElementById('preview').style.display = 'none'
}

function changeImage(direction) {
  currentIndex = (currentIndex + direction + images.length) % images.length
  document.getElementById('previewImg').src = images[currentIndex]
}
</script>
```

#### 手写一个可调整列宽度的表格（resizable table）

```css
<!DOCTYPE html>
<table id="resizableTable" border="1" style="width:100%; table-layout:fixed;">
  <thead>
    <tr>
      <th>列1<div class="resizer"></div></th>
      <th>列2<div class="resizer"></div></th>
      <th>列3<div class="resizer"></div></th>
    </tr>
  </thead>
  <tbody>
    <tr><td>内容1</td><td>内容2</td><td>内容3</td></tr>
    <tr><td>内容4</td><td>内容5</td><td>内容6</td></tr>
  </tbody>
</table>

<style>
th { position:relative; }
.resizer {
  position:absolute;
  right:0;
  top:0;
  width:5px;
  height:100%;
  cursor:col-resize;
  user-select:none;
}
.resizer:hover { background:#ccc; }
</style>

<script>
const table = document.getElementById('resizableTable')
let isResizing = false
let currentCol = null
let startX = 0
let startWidth = 0

table.querySelectorAll('.resizer').forEach((resizer, index) => {
  resizer.addEventListener('mousedown', e => {
    isResizing = true
    currentCol = table.rows[0].cells[index]
    startX = e.pageX
    startWidth = currentCol.offsetWidth
    document.body.style.cursor = 'col-resize'
  })
})

document.addEventListener('mousemove', e => {
  if (!isResizing || !currentCol) return
  const width = startWidth + (e.pageX - startX)
  currentCol.style.width = width + 'px'
  // 同步更新同一列所有单元格
  Array.from(table.rows).forEach(row => {
    if (row.cells[currentCol.cellIndex]) {
      row.cells[currentCol.cellIndex].style.width = width + 'px'
    }
  })
})

document.addEventListener('mouseup', () => {
  isResizing = false
  currentCol = null
  document.body.style.cursor = ''
})
</script>
```

### <font style="color:#4C16B1;">异步与性能控制</font>

#### 使用Promise实现:限制异步操作的并发个数，并尽可能快的完成全部

有8个图片资源的ur，已经存储在数组urls中。

urls类似于\['<https://imagel.png','https://image2.png',....>而且已经有一个函数 function loadImg，输入一个url链接，返回一个Promise，该Promise在图片下载完成的时候resolve，下载失败则reject。

但有一个要求，任何时刻同时下载的链接数量不可以超过3个。

请写一段代码实现这个需求，要求尽可能快速地将所有图片下载完成。

```javascript
var urls = [
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting1.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting2.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting3.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting4.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/AboutMe-painting5.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/bpmn6.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.mygcloud.com/bpmn7.png",
    "https://hexo-blog-1256114407.cos.ap-shenzhen-fsi.myqcloud.com/bpmn8.png",
]
function loadImg(url){
    return new Promise((resolve,reject) => {
        const img = new Image();
        img.onload = function(){
            console.log("一张图片加载完成");
            resolve(img);
        };
        img.onerror =function(){
            reject(new Error('Could not load image at'+ url));
        };
        img.src = url;
    });
}
```

既然题目的要求是保证每次并发请求的数量为3，那么我们可以先请求urs中的前面三个(下标为0,1,2)，并且请求的时候使用 Promise.race()来同时请求，三个中有一个先完成了，我们就把这个当前数组中已经完成的那一项(第1项)换成还没有请求的那一项(urls中下标为3)。

直到urls已经遍历完了，然后将最后三个没有完成的请求(也就是状态没有改变的Promise)用 Promise.all()来加载它们

```javascript
function limitLoad(urls,handler,limit){
    let sequence =[].concat(urls);//复制urls
    // 这-步是为了初始化 promises 这个"容器"
    let promises = sequence.splice(0,limit).map((url,index)=>{
        return handler(url).then(()=>{
            //返回下标是为了知道数组中是哪一项最先完成
            return index;
        });
    });
    //注意这里要将整个变量过程返回，这样得到的就是一个Promise，可以在外面链式调用
    return sequence
        .reduce((pCollect,url)=>{
            return pCollect.then(()=>{
                return Promise.race(promises);//返回已经完成的下标
            }).then(fastestIndex=>{ //获取到已经完成的下标
            // 将"容器"内已经完成的那一项替换
            promises[fastestIndex] = handler(url).then(
                () => {
                    return fastestIndex;//要继续将这个下标返回，以便下一次变量
                }
            );
        })
        .catch(err =>{
            console.error(err);
        });
    }, Promise.resolve()) //初始化传入
    .then(()=>{// 最后三个用.all来调用
        return Promise.all(promises);
    });
}
limitLoad(urls, loadImg, 3)
    .then(res =>{
        console.log("图片全部加载完毕");
        console.log(res);
    })
    .catch(err => {
        console.error(err);
    });

```

#### 用 Promise 实现一个请求重试函数（可设置重试次数和延迟）

```css
async function retry(fn, times = 3, delay = 1000) {
  for(let i = 0; i < times; i++) {
    try {
      return await fn()
    } catch(e) {
      if(i === times - 1) throw e
      await new Promise(r => setTimeout(r, delay))
    }
  }
}
```

#### 用 async/await 实现一个任务队列（按顺序执行异步任务）

```css
class TaskQueue {
  constructor() { this.queue = []; this.running = false }
  async add(task) {
    this.queue.push(task)
    if(!this.running) this.run()
  }
  async run() {
    this.running = true
    while(this.queue.length) await this.queue.shift()()
    this.running = false
  }
}
```

#### 实现一个函数，控制多个请求按顺序发送（一个完成后才发送下一个）

```css
async function serialRequests(urls) {
  for(const url of urls) {
    await fetch(url).then(r => r.json())
  }
}
```

#### 实现一个带缓存的异步请求函数（相同参数请求不重复发）

```css
function cachedFetch() {
  const cache = new Map()
  return async (url) => {
    if(cache.has(url)) return cache.get(url)
    const res = await fetch(url)
    const data = await res.json()
    cache.set(url, data)
    return data
  }
}
```

### <font style="color:#AE146E;">综合场景题</font>

#### 实现一个可以拖拽的div

```javascript
<div id="xxx"></div>

var position = null

xxx.addEventListener('mousedown',function(e){
  dragging = true
  position = [e.clientX, e.clientY]
})


document.addEventListener('mousemove', function(e){
  if(dragging === false) return null
  const x = e.clientX
  const y = e.clientY
  const deltaX = x - position[0]
  const deltaY = y - position[1]
  const left = parseInt(xxx.style.left || 0)
  const top = parseInt(xxx.style.top || 0)
  xxx.style.left = left + deltaX + 'px'
  xxx.style.top = top + deltaY + 'px'
  position = [x, y]
})
document.addEventListener('mouseup', function(e){
  dragging = false;
})
```

#### 实现一个九宫格布局

```css
<!-- - 实现一个九宫格布局 (![](./九宫格效果图.gif))
- 每个单元格都有1px的边框和红黑相间的背景色
- 单元格宽高100px，单元格间间距6px
- hover单元格时，透明度动画过渡到0.5
- hover单元格时，单元格会有变大变小的动画效果 -->
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>九宫格布局</title>
<style>
body {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  margin: 0;
  background-color: #f5f5f5;
}

.grid-container {
  display: grid;//同时控制行和列
  grid-template-columns: repeat(3, 100px);
  grid-template-rows: repeat(3, 100px);
  gap: 6px;
}

.grid-cell {
  width: 100px;
  height: 100px;
  border: 1px solid #000;
  box-sizing: border-box;
  transition: all 0.3s ease;
  display: flex;
  justify-content: center;
  align-items: center;
  font-weight: bold;
  font-size: 24px;
}

/* 红黑相间背景色 */
//奇数
.grid-cell:nth-child(odd) {
  background-color: #ff0000;
  color: white;
}
//偶数
.grid-cell:nth-child(even) {
  background-color: #000000;
  color: white;
}

/* Hover效果 */
.grid-cell:hover {
  opacity: 0.5;
  transform: scale(1.1);//放大1.1倍
}
</style>
</head>
<body>
<div class="grid-container">
<div class="grid-cell">1</div>
<div class="grid-cell">2</div>
<div class="grid-cell">3</div>
<div class="grid-cell">4</div>
<div class="grid-cell">5</div>
<div class="grid-cell">6</div>
<div class="grid-cell">7</div>
<div class="grid-cell">8</div>
<div class="grid-cell">9</div>
</div>
</body>
</html>
```

#### 实现事件委托

```javascript
function delegate(element, eventType, selector, fn) {
    // 参数说明:
    // element: 要绑定事件的父元素 (例如 document.getElementById('list'))
    // eventType: 事件类型 (例如 'click')
    // selector: 一个 CSS 选择器字符串，用于匹配我们真正关心的子元素 (例如 'li.item')
    // fn: 当匹配的子元素触发事件时，要执行的回调函数

    element.addEventListener(eventType, e => {
        // 1. 获取事件的实际触发源
        let el = e.target;

        // 2. 向上遍历 DOM 树，寻找匹配 selector 的元素
        while (!el.matches(selector)) {
            // 3. 边界条件：如果已经遍历到了我们绑定的父元素 element 本身
            if (element === el) {
                el = null; // 说明没找到匹配的子元素，将 el 置为 null
                break;     // 并跳出循环
            }
            // 4. 继续向父节点查找
            el = el.parentNode;
        }

        // 5. 如果找到了匹配的元素 (el 不为 null)，则执行回调函数
        el && fn.call(el, e, el);
    }, true); // <-- 关键点：这里使用了事件捕获

    // 6. 返回父元素，支持链式调用
    return element;
}

```

#### 实现一个简单的虚拟列表（Virtual List），优化长列表渲染性能

```css
class VirtualList {
  constructor(container, items, itemHeight) {
    this.container = container
    this.items = items
    this.itemHeight = itemHeight
    this.visibleCount = Math.ceil(container.clientHeight / itemHeight)
    this.startIndex = 0
    
    this.render()
    container.onscroll = () => {
      this.startIndex = Math.floor(container.scrollTop / itemHeight)
      this.render()
    }
  }
  
  render() {
    const visibleItems = this.items.slice(this.startIndex, this.startIndex + this.visibleCount)
    this.container.innerHTML = visibleItems.map((item, i) => 
      `<div style="height:${this.itemHeight}px">${this.startIndex + i}: ${item}</div>`
    ).join('')
    
    this.container.style.paddingTop = this.startIndex * this.itemHeight + 'px'
  }
}
```

#### 用 Vue 或 React 实现一个可编辑的表格（行内编辑）

```css
<template>
  <table>
    <tr v-for="(row, i) in data" :key="i">
      <td v-for="(cell, j) in row" :key="j" @dblclick="edit(i, j)">
        <input v-if="editing && editing[0]===i && editing[1]===j" 
               v-model="data[i][j]" @blur="stopEdit" @keyup.enter="stopEdit" />
        <span v-else>{{ cell }}</span>
      </td>
    </tr>
  </table>
</template>

<script setup>
import { ref } from 'vue'

const data = ref([
  ['张三', '25', '前端'],
  ['李四', '30', '后端']
])
const editing = ref(null)

const edit = (i, j) => editing.value = [i, j]
const stopEdit = () => editing.value = null
</script>
```

#### 实现一个函数，解析 URL 参数为对象（支持嵌套和数组格式）

```css
function parseURLParams(url) {
  const params = {}
  new URL(url).searchParams.forEach((v, k) => {
    if(k.endsWith('[]')) {
      k = k.slice(0, -2)
      params[k] = params[k] || []
      params[k].push(v)
    } else {
      params[k] = v
    }
  })
  return params
}
```

#### 手写一个简单的路由（hash 模式）

```css
class HashRouter {
  constructor(routes) {
    this.routes = routes
    window.onhashchange = () => this.render()
    window.onload = () => this.render()
  }
  
  render() {
    const hash = location.hash.slice(1) || '/'
    document.getElementById('app').innerHTML = this.routes[hash] || '404'
  }
}
```

####

####

####

####

####

####

####

####


> 更新: 2025-12-14 03:47:18  
> 原文: <https://www.yuque.com/u56987424/lwyx/hbh3vfagkz7o7s93>