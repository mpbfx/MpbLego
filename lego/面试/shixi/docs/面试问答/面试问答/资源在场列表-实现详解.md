# 资源在场列表-实现详解（big-customer，面向面试）

> 目标：把“在场资源池（已入库资源）”的筛选模型、展示聚合、跳转链路、与资源详情/订单联动讲清楚，并能指到代码。

## 1. 一句话概括（面试开场）

资源在场列表是“可立即投入生产的资源池”，核心是：

- **更偏检索与对比**：按译员级别、订单量级、最近接单、价格区间等维度快速筛选
- **信息密度**：一行里展示测试/语言/价格/接单时间/订单数据等
- **联动入口**：跳资源详情、跳简历预览、跳资源历史订单列表

入口页面：`pages/resourceManagement/onSite.vue:1`。

---

## 2. 页面结构：OnSiteFilter + 自定义列 + 表格 + 分页

### A) OnSiteFilter（在场筛选条件更贴近“可用性”）

筛选组件：`components/ResourceFilter/OnSiteFilter.vue:169`  
页面接入：`pages/resourceManagement/onSite.vue:5`

OnSiteFilter 的 form 字段（节选）：`components/ResourceFilter/OnSiteFilter.vue:201`

- 搜索：`searchName`
- 领域/服务/语言对/母语：`domain/service/sourceLanguage/targetLanguage/motherTongue`
- 价格区间：`priceMin/priceMax/priceUnit`
- 在场特有：`translatorLevel`、`orderCount`、`orderTimeRange`、`testScore`、`professionalStatus`
- 资源属性：`resourceType/resourceManager`

并把“筛选展开状态”持久化：`onsite_filter_expanded`（`components/ResourceFilter/OnSiteFilter.vue:195`、`components/ResourceFilter/OnSiteFilter.vue:229`）。

### B) 自定义列（与开发池隔离 key）

在场池自定义列 storage key：`onsite_custom_columns`（`pages/resourceManagement/onSite.vue:18`）。

实现复用 `CustomColumnDropdown`：`components/Table/CustomColumnDropdown.vue:40`。

### C) 表格展示与分页

- 表格主入口：`pages/resourceManagement/onSite.vue:25`（`scroll x=max-content, y=calc(100vh)`）
- 分页器：`pages/resourceManagement/onSite.vue:175`（`handleCurrentChange`）

---

## 3. 接口差异：stored 资源池（在场） vs develop 资源池（开发）

在场池使用 stored 系列接口：

- 默认列表：`getStoredResourcePage`（`config/path.js:28`、`pages/resourceManagement/onSite.vue:700`）
- 条件筛选：`searchStoredResourceListByCondition`（`config/path.js:29`、`pages/resourceManagement/onSite.vue:662`）

对比开发池接口（便于面试说明分池策略）：

- 开发池默认：`getDevelopResourcePage`（`config/path.js:25`）
- 开发池筛选：`searchDevelopResourceListByCondition`（`config/path.js:26`）

对应代码摘录（接口路径枚举）：

```js
// config/path.js:25
// 生产资源管理相关
getSalesTranslatorPage: '/sales/resource/getDevelopResourcePage',
searchResourceListByCondition: '/sales/resource/searchDevelopResourceListByCondition',
```

面试讲法：

- 两个池是“不同生命周期阶段”的资源集合：开发池关注“开发进度/签约”，在场池关注“可用性/接单表现/价格能力”。

---

## 4. 筛选事件与参数映射：UI 字段 → API 字段

### A) 事件入口

页面只接三类事件，并统一走 `applyFiltersWithParams`：

- `handleSearchFromFilter`：`pages/resourceManagement/onSite.vue:599`
- `handleFilterFromFilter`：`pages/resourceManagement/onSite.vue:607`
- `handleResetFromFilter`：`pages/resourceManagement/onSite.vue:619`

### B) 参数映射（在场筛选更丰富）

来源：`pages/resourceManagement/onSite.vue:628`

```js
if (params.searchName) payload.translatorName = params.searchName
if (params.motherTongue) payload.language = params.motherTongue
if (params.relatedProject) payload.associatedProjectName = params.relatedProject
if (params.priceMin !== null) payload.priceLow = String(params.priceMin)
if (params.priceUnit) { payload.currency = parts[0]; payload.unit = parts[1] }
if (params.translatorLevel) payload.level = params.translatorLevel
if (params.orderTimeRange) payload.latestOrderTime = params.orderTimeRange
if (params.testScore) payload.testScore = params.testScore
```

面试讲法：

- UI 的“人类语言维度”（如“一周内/一月内”）最终要映射为后端可理解的字段（这里把它作为枚举值透传给后端）。
- priceUnit 做了拆分：`人民币/字 → currency=人民币, unit=字`，便于后端按字段查询。

---

## 5. 信息密度：decorateListForDisplay 的展示聚合层

在场列表展示信息同样高密度（测试信息、语言水平、接单时间、领域工具、服务价格、项目、订单数据）。  
页面用 `decorateListForDisplay` 做展示聚合：

- 函数入口：`pages/resourceManagement/onSite.vue:254`

其中“订单数据列”合并展示，并给了“查看订单”入口：

- 模板：`pages/resourceManagement/onSite.vue:150`（orderData slot）
- 入口按钮：`pages/resourceManagement/onSite.vue:160`（`goToOrderList(row)`）

面试讲法：

- 在场池需要在列表就能判断“这个资源值不值得用”，所以把关键字段做了聚合展示，减少点进详情的成本。

---

## 6. 联动跳转：详情 / 简历预览 / 历史订单

### A) 跳资源详情（带 from=onsite）

来源：`pages/resourceManagement/onSite.vue:763`

```js
this.$router.push({
  path: `/resourceManagement/resourceDetail/${row.id}`,
  query: { from: 'onsite' }
})
```

### B) 跳简历预览（优先预览最新简历，否则回退到详情简历卡）

来源：`pages/resourceManagement/onSite.vue:770`

- 先查简历文件：`getResourceResumeByResourceIdAndType`（`pages/resourceManagement/onSite.vue:785`）
- 拼下载 url：`pages/resourceManagement/onSite.vue:801`
- 跳预览页：`pages/resourceManagement/onSite.vue:804`
- 无简历则回退到详情页 `#resume`：`pages/resourceManagement/onSite.vue:810`

对应代码摘录：

```js
// pages/resourceManagement/onSite.vue:770
const resumeRes = await this.$http.get(path.getResourceResumeByResourceIdAndType, {
  resourceId: id,
  fileType: 1
})

if (resumeRes.errorCode === 200 && Array.isArray(resumeRes.data) && resumeRes.data.length > 0) {
  const sortedResumes = resumeRes.data.sort((a, b) => {
    const timeA = a.createTime ? new Date(a.createTime).getTime() : 0
    const timeB = b.createTime ? new Date(b.createTime).getTime() : 0
    if (timeA !== timeB) return timeB - timeA
    return (b.id || 0) - (a.id || 0)
  })

  const resumeFile = sortedResumes[0]
  const encodedFileName = encodeURIComponent(resumeFile.fileName || 'resume.pdf')
  const fileUrl = `${path.baseURL}${path.downloadFile}?keyName=${resumeFile.keyName}&fileName=${encodedFileName}`

  this.$router.push({
    path: `/resourceManagement/resumePreview/${id}`,
    query: { url: fileUrl, from: 'onsite' }
  })
} else {
  this.$router.push({ path: `/resourceManagement/resourceDetail/${id}`, hash: '#resume', query: { from: 'onsite' } })
  this.$message.warning('暂无简历文件')
}
```

这条链路的“下载/预览细节”见专题：`docs/面试问答/上传下载-文件预览-详解.md:1`。

### C) 跳资源历史订单列表

来源：`pages/resourceManagement/onSite.vue:842`

```js
this.$router.push({
  path: '/resourceManagement/resourceDetail/orderRecords',
  query: { resourceId: row.id, resourceName: row.translatorName, from: 'onsite' }
})
```

面试讲法：

- 在场池的使用场景里，“看历史订单/合作量级/最近接单”是高频，因此提供一跳直达，减少多次点击。

---

## 7. URL 参数回填：从外部入口带筛选条件直达在场列表

页面 mounted 时会把 route.query 转换成筛选 form 并回填到筛选组件，再直接触发筛选请求：

- query → filterParams：`pages/resourceManagement/onSite.vue:564`
- 回填到筛选组件：`pages/resourceManagement/onSite.vue:582`～`pages/resourceManagement/onSite.vue:588`

面试讲法：

- 这种“URL 驱动筛选态”的方式便于从报表/统计卡片跳转到在场列表，并且可分享链接复现筛选结果。

---

## 8. 常见坑 & 可改进点（面试加分）

1) 在场列表与开发列表都有 `decorateListForDisplay`（各自一份）：可以抽到 `utils/resource/listDecorators` 并做单测，避免两边格式不一致。
2) 筛选项“枚举值”现在分散在 OnSiteFilter（如 orderTimeRangeOptions）：建议统一在 `config/`，便于前后端一致。
3) 列表内的 tooltip + clamp 依赖样式：如果列变多或行高变大，需要统一设计“信息密度策略”（只展示最近 N 条、tooltip 展示 M 条）。

---

## 补充：主导职责、量化结果、复盘与分时长回答

### 主导职责（可直接说）

在「资源模块建模与状态流转」这部分，我主导完成了：方案拆解、风险评估、核心代码落地、联调上线与复盘沉淀。主导范围覆盖：字典枚举治理、字段权限、状态机联动。

### 量化结果（请按真实数据替换）

- 关键指标：字段映射错误率、状态流转异常数、页面回归缺陷 从 X 优化到 Y。
- 交付效率：同类需求交付周期从 X 天 缩短到 Y 天。
- 稳定性：相关线上问题从 X 个/迭代下降到 Y 个/迭代。
- 可维护性：重复逻辑与临时补丁占比下降 X%。

### 故障复盘卡片（与本文主题相关）

1) 现象：字典映射错误导致展示/提交异常。  
- 影响：核心流程可用性或数据一致性受影响。  
- 定位：通过日志、埋点、接口返回与代码链路回溯定位到根因。  
- 止血：快速回滚/降级并加兜底判断。  
- 长期修复：补充边界校验、自动化测试与发布前检查项。

2) 现象：改造后出现跨模块联动异常。  
- 影响：上下游模块结果不一致。  
- 定位：接口契约、状态同步或配置项存在偏差。  
- 止血：统一契约并临时加兼容转换。  
- 长期修复：把契约收敛为单一来源并补文档与门禁。

3) 现象：高峰期下性能或失败率波动。  
- 影响：用户体验下降，工单增加。  
- 定位：识别瓶颈点（请求并发、渲染、缓存或鉴权链路）。  
- 止血：限流/重试/懒加载或拆分重任务。  
- 长期修复：建立持续监控与阈值告警，按周复盘。

### 分时长回答（背诵版）

- 30 秒：
  这部分是我主导落地的，核心目标是把「资源模块建模与状态流转」做到稳定、可扩展、可复用，重点解决一致性和线上可维护性。

- 90 秒：
  我按“问题-方案-结果”推进：先定义边界与关键风险，再做结构化改造和统一约定，最后用测试、监控和流程门禁固化成果，确保同类问题不反复出现。

- 3 分钟：
  按“背景、挑战、方案、落地、结果、复盘”展开：
  1) 业务背景与约束。  
  2) 当前痛点与失败案例。  
  3) 方案选择与取舍依据。  
  4) 实施路径与跨团队配合。  
  5) 指标结果与后续优化计划。
